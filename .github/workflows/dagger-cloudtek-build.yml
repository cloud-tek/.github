name: "dagger"
on:
  workflow_call:
    outputs:
      artifact-name:
        description: "The name of the uploaded artifact"
        value: ${{ jobs.dagger.outputs.artifact-name }}
    secrets:
      NUGET_API_KEY:
        description: "NuGet API Key"
        required: false
      GH_TOKEN:
        description: "GitHub Token"
        required: false
      DAGGER_CLOUD_TOKEN:
        description: "Dagger Cloud Token"
        required: false
      DAGGER_SSH_PRIVATE_KEY:
        description: "dagger@cloud-tek.io key"
        required: true
    inputs:
      directory:
        description: "Project directory"
        type: string
        required: true
      net-version:
        description: ".NET version"
        type: string
        default: "10.0"
        required: false
      net-sdk-version:
        description: ".NET SDK version"
        type: string
        default: "10.0.100"
        required: false
      dagger-module:
        description: "Dagger Module"
        type: string
        default: "github.com/cloud-tek/dagger/dotnet/cloudtek-build@v0.2.0"
        required: false
      cloudtek-build-version:
        description: "CloudTek.Build.Tool version"
        type: string
        default: "10.0.0"
        required: false
      target:
        description: "NUKE Target"
        type: string
        default: "All"
        required: false
      skip:
        description: "NUKE Skip Targets"
        type: string
        default: ""
        required: false
      docker-socket:
        description: "Enable Docker-in-Docker for the build (e.g. for Testcontainers)"
        type: boolean
        default: false
        required: false

jobs:
  dagger:
    name: dagger
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.artifact-name.outputs.name }}
    env:
      VAR_ARTIFACT_NAME: artifacts
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: "0"
          token: ${{ secrets.GH_TOKEN }}
      - name: 'git config'
        run: |
          git config --global url."git@github.com:".insteadOf https://github.com
      - name: 'ssh-agent'
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DAGGER_SSH_PRIVATE_KEY }}
      - name: global.json
        shell: bash
        run: |
          if [ -f "${{ github.workspace }}/global.json" ]; then
            echo "global.json exists"
          else
            echo "{ \"tools\": { \"dotnet\": \"${{ inputs.net-sdk-version }}\" } }" > ${{ github.workspace }}/global.json
          fi
      # registry.dagger.io is unreachable from GitHub Actions runners (connection timeouts on
      # GET /v2/). Prime the engine from cloud-tek's ghcr mirror (ghcr.io IS reachable) by
      # pulling it and retagging to the exact ref the Dagger CLI provisioner looks for. The
      # provisioner does `docker image inspect registry.dagger.io/engine:<ver>` first and only
      # pulls on a cache miss, so with the image present locally it never contacts the network.
      - name: 'docker login (ghcr.io engine mirror)'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GH_TOKEN }}
      - name: 'prime dagger engine from ghcr mirror'
        shell: bash
        run: |
          docker pull ghcr.io/cloud-tek/dagger-engine:v0.21.7
          docker tag ghcr.io/cloud-tek/dagger-engine:v0.21.7 registry.dagger.io/engine:v0.21.7
      - name: dagger call
        uses: dagger/dagger-for-github@v8.2.0
        with:
          # CLI version MUST match the primed engine tag (registry.dagger.io/engine:v0.21.7).
          version: "v0.21.7"
          module: ${{ inputs.dagger-module }}
          args: >-
            build
            --source ${{ github.workspace }}/${{ inputs.directory }}
            --target ${{ inputs.target }}
            --skip "${{ inputs.skip }}"
            --toolVersion ${{ inputs.cloudtek-build-version }}
            --sdkVersion ${{ inputs.net-sdk-version }}
            --buildNumber ${{ github.run_number }}
            --collectCoverage
            ${{ inputs.docker-socket && '--with-docker' || '' }}
            export --path ${{ github.workspace }}/${{ inputs.directory }}
          cloud-token: ${{ secrets.DAGGER_CLOUD_TOKEN }}

      - name: tree results
        run: |
          tree ${{ github.workspace }}/results
      - name: tree artifacts
        if: ${{ !(contains(inputs.skip, 'Pack') && contains(inputs.skip, 'Publish')) }}
        run: |
          tree ${{ github.workspace }}/artifacts

      - name: set artifact name
        id: artifact-name
        run: |
          echo "name=${{ env.VAR_ARTIFACT_NAME }}" >> $GITHUB_OUTPUT

      - name: upload-artifacts
        uses: actions/upload-artifact@v5
        if: ${{ !(contains(inputs.skip, 'Pack') && contains(inputs.skip, 'Publish')) }}
        with:
          name: ${{ env.VAR_ARTIFACT_NAME }}
          path: ${{ github.workspace }}/artifacts
          if-no-files-found: error

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: (success() || failure())
        with:
          name: XUnit Tests
          path: ${{ github.workspace }}/results/tests/*.trx
          reporter: dotnet-trx
